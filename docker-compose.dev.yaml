services:
  yt-dlp-service:
    command: [
      "uvicorn",
      "src.api_gateway.main:app",
      "--host", "0.0.0.0",
      "--port", "8000",
      "--reload"
    ]
    volumes:
      - ./src:/app/src       # 挂载源码 → 支持热重载
